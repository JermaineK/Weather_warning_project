# Core numeric + data
numpy
pandas
xarray
scipy
scikit-learn
lightgbm
joblib
pyarrow

# Plotting + mapping
matplotlib
cartopy
shapely
pyproj
imageio

# I/O + config
pyyaml
requests
tqdm
dask
zstandard     # faster parquet/csv.gz support if available
psutil        # runtime memory sizing for chunk selection

# Optional extras used in pipeline scripts
netCDF4       # for direct .nc access (build_features_grid.py / ERA5)
cfgrib        # allows reading GRIB files via xarray if ERA5 switches format
bottleneck    # speeds up rolling ops in pandas/xarray
seaborn       # optional diagnostic plots

# Testing / notebooks (optional but recommended)
jupyter
